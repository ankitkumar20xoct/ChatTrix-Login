window.addEventListener('DOMContentLoaded', () => {
  const element = document.getElementById('uppercaseBox');
  if (element) {
    const text = element.innerText;
    element.innerText = text.toUpperCase();
    console.log('Uppercase directive works — text converted to UPPERCASE');
  }
});
