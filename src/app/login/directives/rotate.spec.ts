import { RotateDirective } from './rotate';

describe('RotateDirective', () => {
  it('should create an instance', () => {
    const directive = new RotateDirective();
    expect(directive).toBeTruthy();
  });
});